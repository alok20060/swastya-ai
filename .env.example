# Copy this to .env and fill in your values

# Your backend API base URL (no trailing slash)
# In development with Vite proxy, leave as /api
# In production, set to full URL e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
